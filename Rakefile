# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'framecurve' do
  # developer('FIX', 'FIX@example.com')

  # self.rubyforge_name = 'framecurvex' # if different than 'framecurve'
end

# vim: syntax=ruby
